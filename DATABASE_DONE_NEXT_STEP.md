# 데이터베이스 설정 완료! ✅

**상태:** 데이터베이스 및 사용자 생성 완료!

---

## ✅ 완료된 작업

- ✅ 데이터베이스 `seoom` 생성 완료
- ✅ 사용자 `seoom_user` 생성 완료
- ✅ 비밀번호: `Tpdk1021!` (기억해두세요!)
- ✅ 권한 부여 완료
- ✅ 다음 단계 준비 완료

---

## 🎯 다음 단계: Apache 설치 (6-7 단계)

Apache는 웹 서버예요. 웹사이트를 인터넷에 공개하는 데 필요해요!

### 1단계: Apache 설치

```bash
sudo apt install -y apache2
```

Enter 키 누르고 완료될 때까지 기다리세요.  
**예상 시간:** 1-2분

### 2단계: PHP-FPM 모듈 활성화

```bash
sudo a2enmod proxy_fcgi setenvif
```

Enter 키 누르기

### 3단계: PHP-FPM 설정 활성화

```bash
sudo a2enconf php8.3-fpm
```

Enter 키 누르기

### 4단계: 필수 모듈 활성화

```bash
sudo a2enmod rewrite
```

Enter 키 누르기

```bash
sudo a2enmod ssl
```

Enter 키 누르기

```bash
sudo a2enmod headers
```

Enter 키 누르기

### 5단계: Apache 재시작

```bash
sudo systemctl restart apache2
```

Enter 키 누르기

### 6단계: Apache 상태 확인

```bash
sudo systemctl status apache2
```

Enter 키 누르면 Apache 상태가 보여요.

**종료:** `q` 키 누르기

---

## 📋 전체 명령어 (복사해서 사용)

```bash
# Apache 설치
sudo apt install -y apache2

# PHP-FPM 모듈 활성화
sudo a2enmod proxy_fcgi setenvif

# PHP-FPM 설정 활성화
sudo a2enconf php8.3-fpm

# 필수 모듈 활성화
sudo a2enmod rewrite
sudo a2enmod ssl
sudo a2enmod headers

# Apache 재시작
sudo systemctl restart apache2

# Apache 상태 확인
sudo systemctl status apache2
```

---

## 💡 팁

- 명령어는 **하나씩** 입력하세요
- 각 명령어가 완료될 때까지 기다리세요
- `a2enmod`는 Apache 모듈을 활성화하는 명령어예요

---

## ✅ 완료 확인

Apache 상태 확인에서:
- `active (running)`이 보이면 성공! ✅
- 에러가 나오면 알려주세요

---

## 🎯 다음 단계 (Apache 설치 완료 후)

1. **Git 설치** (6-8 단계)
2. **프로젝트 업로드** (6-9 단계)
3. **Laravel 설정** (6-10 단계)
4. **Apache 가상 호스트 설정** (6-11 단계)

---

**Apache 설치 명령어를 실행하세요!**

